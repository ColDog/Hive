%table.table
  %thead
    %tr
      - header.each do |header|
        %td
          = header
    - data.each do |data|
      %tr
        - methods.each do |method|
          %td
            = data.send(method)
        %td
          - links.each do |link|
            = link_to link[0], link[1]